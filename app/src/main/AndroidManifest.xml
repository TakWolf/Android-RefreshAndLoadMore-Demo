<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.takwolf.android.demo.refreshandloadmore">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.App"
        android:allowBackup="true"
        android:fullBackupContent="true">

        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.PhotoListActivity" />

        <activity android:name=".activity.PhotoListNotFullActivity" />

        <activity android:name=".activity.CNodeActivity" />

        <activity android:name=".activity.CNodeNotFullActivity" />

        <activity android:name=".activity.ZhihuActivity" />

        <activity android:name=".activity.ZhihuNotFullActivity" />

    </application>

</manifest>
